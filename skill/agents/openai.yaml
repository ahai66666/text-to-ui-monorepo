interface:
  display_name: "Text to UI"
  short_description: "Build HarmonyOS PC pages from registered native components"
  default_prompt: "Use $text-to-ui to build my HarmonyOS PC tool. Apply the required source order for every UI region: import a real HTML, React, or Vue library component when available; otherwise implement from the matching canonical contract; only when both are absent draw a custom component with shared Tokens and HarmonyOS PC rules. Record and validate every source decision before browser and Pixso QA."
