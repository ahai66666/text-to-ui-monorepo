# 设计策略

页面以高密度生产力工具为目标：浅灰桌面背景承载白色圆角主工作区，边界和阴影保持克制；文本层级通过字号、字重和中性色建立，不依赖大面积装饰色。蓝色只用于主操作、选中状态和未读提示。

三栏按信息流由左向右递进：应用与文件夹导航 → 邮件选择 → 内容阅读。列表项采用真实 `ListCard` 适配器根节点，在其 slot 语义内填充邮件字段；按钮、搜索、标题栏、复选框、半模态、附件和 Toast 均由生产组件适配器实例化后组合。

当前页面不参考旧邮箱产品的视觉或 DOM，只使用本次确认的信息架构、HarmonyOS PC 设计语言和组件仓契约。
